extends layout

block content
    h2 Períodos
    .w3-container
        each compositores,nome in periodos
            div.w3-card-4
                header.w3-container.w3-orange
                    h1= nome
                .w3-container
                    each compositor in compositores
                        p
                            a(href='/compositores/' + compositor.id)= compositor.nome
